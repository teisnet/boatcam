extends layout

block navigation
    +breadcrumb('cameras')

block content
    .page-header
        .status-indicator
        = title
    .video-window
        video(src= camera.uri.replace("rtmp://", "http://") + "/playlist.m3u8" controls="" autoplay="" webkit-playsinline)
